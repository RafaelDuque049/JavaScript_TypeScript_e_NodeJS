var name1; // A variável 'VAR' pode ser utilizada globalmente; (Uso não recomendado)

let name2; // Ele é gerado dentro de um escopo;

const name3 = 'nome'; // Ele é gerado dentro de um escopo, e pode receber dados apenas uma vez.(lançara um erro assim que tentar declarar um novo valor)


var cachorro = "Kyara";
console.log(cachorro)

let lanche = "Bolovo";
console.log(lanche)

const nome = "Dener";
console.log(nome)
